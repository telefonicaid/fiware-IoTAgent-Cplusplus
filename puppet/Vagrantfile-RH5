# -*- mode: ruby -*-
# vi: set ft=ruby :
# Vagrant file for development purposes
Vagrant::Config.run do |config|
 config.vm.box = "RH5.5"
 config.vm.box_url = "http://cruzyraya.aislada.hi.inet/~user_rse01/rh5.5_base.box"
 config.vm.forward_port 80, 8080
 config.vm.boot_mode = :headless
end

