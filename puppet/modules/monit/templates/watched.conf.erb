########################
# FILE MANAGED BY PUPPET
########################

# --------------
# <%= @title %>
# --------------

check process <%= @title %> with pidfile <%= @pidfile %>
   start program  = "<%= @start_cmd %>" as uid "<%= @user %>" and gid "<%= @group %>"
   stop  program  = "<%= @stop_cmd %>" as uid "<%= @user %>" and gid "<%= @group %>"
